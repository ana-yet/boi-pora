export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum BookStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum LibraryItemStatus {
  SAVED = 'saved',
  READING = 'reading',
  FINISHED = 'finished',
}
